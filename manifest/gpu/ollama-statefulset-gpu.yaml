apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ollama
  namespace: local-ai-lab
spec:
  selector:
    matchLabels:
      app: ollama
  serviceName: "ollama"
  template:
    spec:
      runtimeClassName: nvidia
      containers:
      - name: ollama
        resources:
          limits:
            nvidia.com/gpu: "1"
